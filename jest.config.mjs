/** @type {import('jest').Config} */
const config = {
  projects: [
    "<rootDir>/projects/eta-extended/jest.config.mjs",
    "<rootDir>/projects/nu-html-checker/jest.config.mjs",
  ],
};

export default config;
